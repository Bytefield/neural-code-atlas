This is a file with no frontmatter at all.

It just has regular markdown content split across multiple paragraphs.

The parser should handle this gracefully and derive the ID from the filename.

All optional fields should be undefined and status should default to vigente.
