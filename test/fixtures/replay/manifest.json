{
  "fixture": "nca_ask replay — Phase A exit gate",
  "purpose": "Frozen replay of the 64 nca_ask calls from the SYNIO Phase 0 baseline corpus (main-only cwd), against a frozen SYNIO index, to demonstrate the Phase A exit gate deterministically across builds. Test-side only — not a product command.",

  "queries": {
    "file": "test/fixtures/replay/queries.jsonl",
    "count": 64,
    "source_corpus": "~/.nca/metrics/synio/orientation-events.jsonl",
    "source_manifest": "~/.nca/metrics/synio/orientation-events.manifest.json",
    "source_manifest_temporal_range": {
      "earliest": "2026-05-28T09:03:20.251Z",
      "latest": "2026-06-26T07:10:32.860Z"
    },
    "filter": "tool_name == mcp__nca__nca_ask AND event_type == post_tool_use (all 64 such events in the frozen orientation corpus — no additional window or filtering applied; this is the corpus's own main-only, cwd-filtered baseline set)",
    "reconstruction_method": "Orientation events strip content by design (privacy scrub) and only carry a deterministic event_id = sha256(source_project|session_id|line.timestamp|'post_tool_use'|tool_use_id)[:16] (see src/corpus/events.ts:makeEventId). Recomputed this hash for every mcp__nca__nca_ask tool_use block found in the raw session transcripts backed up at ~/nca-corpus-backup/-mnt-c-dev-synio/ (source_project='synio', matching the manifest's --project value), and kept the block whose recomputed event_id matched one of the 64 target event_ids. Result: 64/64 matched bijectively, 0 collisions, 0 missing — no ambiguity, no window had to be chosen by hand.",
    "raw_transcripts_note": "Raw session transcripts for this corpus had already rotated out of ~/.claude/projects/-mnt-c-dev-synio/ by the time this fixture was built; recovered from a prior local backup at ~/nca-corpus-backup/-mnt-c-dev-synio/ (148 session files). This backup is external to this repo and not itself part of the fixture — only its extract (queries.jsonl) is frozen here.",
    "caveat_out_of_scope_project_args": "6 of the 64 recorded calls passed an explicit `project` argument pointing outside the SYNIO codebase: 5 at the Obsidian vault mirror '/mnt/c/Users/jesus/Desktop/Papi_Obsidian_Vault/02-Projects/synio' (that index still exists on disk as of fixture creation, but is NOT frozen by this fixture — it is live, mutable, external state), and 1 at '/mnt/c/dev/webs/synio-web' (that index no longer exists on disk as of fixture creation, so it will resolve to a known_env_error 'No NCA index found' today, which it did NOT do at baseline time — it returned real no-match-fallback content back then). Per task instructions these 6 queries were kept unmodified — not filtered — since the fixture must reproduce the original 64 calls verbatim. The harness's throwaway NCA_REGISTRY_PATH only contains a 'synio' entry, so these 6 do not resolve through it; they fall through to nca_ask's own direct-path resolution and, for the 5 that still exist, hit the REAL live vault index directly (including its normal logQuery/updateNodeScores writes — the same trivial, additive side effect any real nca_ask call against that vault already produces day to day). Their classification under replay is therefore NOT guaranteed stable across time or machines; every report generated by this harness flags them individually. This is a known, unresolved discrepancy — see report.md."
  },

  "index": {
    "project": "synio",
    "root": "/mnt/c/dev/synio",
    "description": "Snapshot of the SYNIO project's *current* NCA index at fixture-creation time (not a historical reconstruction of what the index looked like during the original May-June 2026 baseline sessions — the task asked for the current index).",
    "synio_commit": "e29cb7a12d6ac860f8175fa478e88147c4ba1ba7",
    "synio_branch": "chore/dedicated-servers-infra",
    "synio_commit_date": "2026-09-02T18:43:20+02:00",
    "schema_version": 4,
    "node_count": 2795,
    "migration_log": [
      { "version": 1, "name": "init_schema", "applied_at": "2026-06-07T21:02:21Z" },
      { "version": 2, "name": "repair_line_move_duplicates", "applied_at": "2026-06-07T21:02:21Z" },
      { "version": 3, "name": "vault_schema", "applied_at": "2026-06-07T21:02:21Z" },
      { "version": 4, "name": "doc_code_edges", "applied_at": "2026-06-07T21:02:21Z" }
    ],
    "generator_nca_version_note": "Not tracked by the DB itself (no generator-version column). Inferred from schema_version=4 (requires a build with migrations/004_doc_code_edges.ts) and the migration_log applied_at timestamp (2026-06-07), which predates both #54 and #55 — those two fixes do not touch migrations/, so they do not affect this index's schema.",
    "storage": {
      "size_bytes": 7233536,
      "size_mb": 6.9,
      "rule_applied": "size >= 5MB threshold -> stored OUTSIDE the repo",
      "path": "~/.nca/replay/synio-frozen.db",
      "sha256": "9411ac277f070c00736c4e86816fda55396dd53f80bdb3436e424e6b0e5718a4",
      "source_path_at_freeze_time": "/mnt/c/dev/synio/.nca/nca.db",
      "permissions": "chmod 444 (read-only) to reduce accidental mutation; the harness never opens this file directly — it copies it into a fresh tmpdir per run before spawning the MCP server, since toolAsk writes query-log/score-boost rows on every call and would otherwise mutate the frozen fixture."
    }
  },

  "harness": {
    "invocation": "node test/replay/run.js [--dist <path-to-dist-dir>]",
    "default_dist": "<repo-root>/dist",
    "call_path": "Spawns `node <dist>/mcp.js` as a child process and drives it over the real stdio JSON-RPC transport (tools/call, name: nca_ask) — the same transport Claude Code itself uses to talk to this MCP server. This is what 'the same path agents see' means here: mcp.ts has no exports (it calls main() unconditionally at module load and binds process.stdin), so there is no in-process function to import without either touching src/ or hijacking the harness's own stdin. AC5 (test/run.js) validates the same server the same way, over the same transport, for the same reason.",
    "isolation": "NCA_DB_PATH and NCA_REGISTRY_PATH point the spawned server at a per-run tmpdir copy of the frozen index and a throwaway single-entry registry ({name: 'synio', root: '/mnt/c/dev/synio', dbPath: <tmp copy>}), so queries carrying project_arg=null or project_arg='/mnt/c/dev/synio' both resolve to the frozen copy, and Migue's real, live /mnt/c/dev/synio/.nca/nca.db is never opened or touched by this harness.",
    "determinism_normalization": "Two fields in nca_ask's text output are wall-clock-derived and therefore excluded from any stored/compared content, normalized before classification, golden storage, and diffing (this manifest is the only place absolute run times are recorded, matching the corpus extractor's own convention): (1) the `|t:<epoch_ms>` timestamp token embedded by every NCA|q:... header line -> normalized to `|t:<TS>`; (2) the `[WARN] Index is <N> days old` staleness line (src/mcp.ts:staleWarning, computed from Date.now() vs the frozen DB's fixed parsed_at) -> normalized to `[WARN] Index is <DAYS> days old`, since <N> grows every day this fixture exists and would otherwise break byte-exact comparison indefinitely."
  },

  "classifier": {
    "direct_hit": "JSON-RPC success AND output does NOT contain \"no matches for '\" — real symbol/path data returned for the query.",
    "clean_no_match": "JSON-RPC success AND output contains \"no matches for '\" AND output contains NO [F] section AND output contains no flow-shaped content (no `#name[steps]` pattern).",
    "noisy_fallback": "JSON-RPC success AND output contains \"no matches for '\" AND output DOES contain a [F] section or flow-shaped content — a miss dressed up as an answer (the #54 bug).",
    "known_env_error": "JSON-RPC error whose message matches /NCA schema version mismatch/ (SchemaVersionSkewError, #55) OR the pre-#55 raw form /schema_version \\(\\d+\\) is newer than this build supports/ OR /No NCA index found/ — classified, not counted as a product defect.",
    "product_error": "Any other JSON-RPC error, or a missing/malformed response."
  },

  "gate": {
    "noisy_fallback_count": "must be 0",
    "product_error_count": "must be 0 (known_env_error excluded)",
    "direct_hit_count": "must be >= 12, and content must be byte-identical (after normalization) to the pre-fix golden output for the same query",
    "clean_no_match": "every non-hit, non-error query must land in clean_no_match, not noisy_fallback"
  }
}
